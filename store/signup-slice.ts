import { createSlice, PayloadAction } from '@reduxjs/toolkit';

interface signup {

};
const initialState = {

}
const Signup_Slice = createSlice({
    name: 'signup',
    initialState,
    reducers: {}
})